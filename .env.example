# ===========================================
# uuidy Configuration
# ===========================================
# Copy this file to .env and update values as needed

# Database connection string (PostgreSQL with asyncpg driver)
# Format: postgresql+asyncpg://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql+asyncpg://uuidy:uuidy@localhost:5432/uuidy

# SerpAPI key for Google search integration
# Get your key at: https://serpapi.com/
# Leave empty or omit for graceful fallback (searches will return "Unknown")
SERPAPI_KEY=

# Cache TTL (Time To Live) in days
# How long to keep cached classification records before they expire
# Default: 30
CACHE_TTL_DAYS=30
